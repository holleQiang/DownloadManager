package com.zhangqiang.downloadmanager.db.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zhangqiang.downloadmanager.db.entity.HttpPartTaskEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "HTTP_PART_TASK_ENTITY".
*/
public class HttpPartTaskEntityDao extends AbstractDao<HttpPartTaskEntity, String> {

    public static final String TABLENAME = "HTTP_PART_TASK_ENTITY";

    /**
     * Properties of entity HttpPartTaskEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", true, "ID");
        public final static Property State = new Property(1, int.class, "state", false, "STATE");
        public final static Property CreateTime = new Property(2, long.class, "createTime", false, "CREATE_TIME");
        public final static Property ErrorMsg = new Property(3, String.class, "errorMsg", false, "ERROR_MSG");
        public final static Property ItemIds = new Property(4, String.class, "itemIds", false, "ITEM_IDS");
    }


    public HttpPartTaskEntityDao(DaoConfig config) {
        super(config);
    }
    
    public HttpPartTaskEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"HTTP_PART_TASK_ENTITY\" (" + //
                "\"ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: id
                "\"STATE\" INTEGER NOT NULL ," + // 1: state
                "\"CREATE_TIME\" INTEGER NOT NULL ," + // 2: createTime
                "\"ERROR_MSG\" TEXT," + // 3: errorMsg
                "\"ITEM_IDS\" TEXT);"); // 4: itemIds
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"HTTP_PART_TASK_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, HttpPartTaskEntity entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
        stmt.bindLong(2, entity.getState());
        stmt.bindLong(3, entity.getCreateTime());
 
        String errorMsg = entity.getErrorMsg();
        if (errorMsg != null) {
            stmt.bindString(4, errorMsg);
        }
 
        String itemIds = entity.getItemIds();
        if (itemIds != null) {
            stmt.bindString(5, itemIds);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, HttpPartTaskEntity entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
        stmt.bindLong(2, entity.getState());
        stmt.bindLong(3, entity.getCreateTime());
 
        String errorMsg = entity.getErrorMsg();
        if (errorMsg != null) {
            stmt.bindString(4, errorMsg);
        }
 
        String itemIds = entity.getItemIds();
        if (itemIds != null) {
            stmt.bindString(5, itemIds);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public HttpPartTaskEntity readEntity(Cursor cursor, int offset) {
        HttpPartTaskEntity entity = new HttpPartTaskEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            cursor.getInt(offset + 1), // state
            cursor.getLong(offset + 2), // createTime
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // errorMsg
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // itemIds
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, HttpPartTaskEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setState(cursor.getInt(offset + 1));
        entity.setCreateTime(cursor.getLong(offset + 2));
        entity.setErrorMsg(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setItemIds(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final String updateKeyAfterInsert(HttpPartTaskEntity entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public String getKey(HttpPartTaskEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(HttpPartTaskEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
