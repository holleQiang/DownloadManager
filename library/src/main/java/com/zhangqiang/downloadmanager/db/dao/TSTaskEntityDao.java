package com.zhangqiang.downloadmanager.db.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zhangqiang.downloadmanager.db.entity.TSTaskEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TSTASK_ENTITY".
*/
public class TSTaskEntityDao extends AbstractDao<TSTaskEntity, String> {

    public static final String TABLENAME = "TSTASK_ENTITY";

    /**
     * Properties of entity TSTaskEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", true, "ID");
        public final static Property Uri = new Property(1, String.class, "uri", false, "URI");
        public final static Property Duration = new Property(2, float.class, "duration", false, "DURATION");
        public final static Property ChildId = new Property(3, String.class, "childId", false, "CHILD_ID");
    }


    public TSTaskEntityDao(DaoConfig config) {
        super(config);
    }
    
    public TSTaskEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TSTASK_ENTITY\" (" + //
                "\"ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: id
                "\"URI\" TEXT NOT NULL ," + // 1: uri
                "\"DURATION\" REAL NOT NULL ," + // 2: duration
                "\"CHILD_ID\" TEXT);"); // 3: childId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TSTASK_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TSTaskEntity entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
        stmt.bindString(2, entity.getUri());
        stmt.bindDouble(3, entity.getDuration());
 
        String childId = entity.getChildId();
        if (childId != null) {
            stmt.bindString(4, childId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TSTaskEntity entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
        stmt.bindString(2, entity.getUri());
        stmt.bindDouble(3, entity.getDuration());
 
        String childId = entity.getChildId();
        if (childId != null) {
            stmt.bindString(4, childId);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public TSTaskEntity readEntity(Cursor cursor, int offset) {
        TSTaskEntity entity = new TSTaskEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            cursor.getString(offset + 1), // uri
            cursor.getFloat(offset + 2), // duration
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // childId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TSTaskEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setUri(cursor.getString(offset + 1));
        entity.setDuration(cursor.getFloat(offset + 2));
        entity.setChildId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final String updateKeyAfterInsert(TSTaskEntity entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public String getKey(TSTaskEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(TSTaskEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
